/**
 * WordPress dependencies
 */
import { useBlockProps } from '@wordpress/block-editor';

/**
 * Save function for Property Listings block
 * This returns null because we use PHP rendering
 */
export default function save() {
	return null;
}